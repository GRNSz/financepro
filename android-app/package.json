{
  "name": "finance-app-android",
  "version": "1.1.2",
  "description": "Versão Android do Controle Financeiro",
  "scripts": {
    "sync": "npx cap sync",
    "build-apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor-firebase/authentication": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
