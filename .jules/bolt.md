## 2026-08-12 - [O(N) Hash Map for Chart Aggregation]
**Learning:** [Parsing native Date objects in large loops causes performance bottlenecks. Prefer O(N) hash map aggregation with fast string slicing (substring) on dates.]
**Action:** [Use hash map aggregation and substring for dates instead of nested loops and Date parsing.]
